{
    "name": "debgen",
    "version": "1.0.0",
    "description": "Debian Sources List Generator - A web application to generate sources.list files for Debian and Ubuntu systems",
    "main": "src/index.js",
    "scripts": {
        "build": "webpack --config webpack.config.js",
        "dev": "webpack-dev-server --config webpack.config.js",
        "start": "npm run dev",
        "test": "echo \"No tests specified\" && exit 0",
        "lint": "echo \"No linting specified\" && exit 0",
        "deploy": "npm run build && echo 'Build completed! Upload public/ folder to Cloudflare Pages'",
        "deploy:wrangler": "npm run build && wrangler pages deploy public --project-name=debgen"
    },
    "keywords": ["debian", "ubuntu", "sources", "list", "generator", "apt", "repository"],
    "author": "gOOvER",
    "license": "MIT",
    "dependencies": {
        "i18next": "^25.6.0",
        "i18next-browser-languagedetector": "^8.2.0",
        "i18next-http-backend": "^3.0.2"
    },
    "devDependencies": {
        "webpack": "^5.95.0",
        "webpack-cli": "^5.1.4",
        "webpack-dev-server": "^5.1.0"
    }
}
